#!/usr/bin/env node
/**
 * Placeholder for downloading ML artifacts in Docker/production builds.
 * Extend this to pull model weights from S3 or another store when needed.
 */
console.log('download-models: skip (no external models required for dev)');
process.exit(0);
